## TYPESCRIPT

### Dictionary

Create New File :
`$ echo {} > tsconfig.json && tsc`

Compile ts file :
`tsc`

Setup Output Directory for js after compiler :
```javascript
{
  "compilerOptions": {
    "outDir": "dist"
  }
}
```